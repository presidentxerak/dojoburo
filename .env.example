# Optional: Xaman (XUMM) API key for secure non-custodial Mainnet signing.
# Get a free key at https://apps.xaman.dev (only the API KEY is needed —
# never put the API secret in a frontend app). You can also paste the key
# directly in the app's Xaman panel instead of using this file.
VITE_XUMM_API_KEY=
